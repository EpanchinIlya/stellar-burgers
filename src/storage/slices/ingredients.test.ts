import { getIngredientsApi } from '../../utils/burger-api';
import { TIngredient } from '../../utils/types';
import { apis } from '../thunk/ingredient';
//import { api } from '../thunk/ingredient';

import mockIngredients from '../../__mocks__/ingredients.json';


describe('Тесты для slice ingredients', () => {
  test('Тест async fetchIngredients', () => {
    // const spy = jest
    //   .spyOn(api, 'fetchIngredients')
    //   .mockResolvedValue(mockIngredients);
    //await store.dispatch(getIngredients());
  });

  test('Тест async fetchIngredients', () => {
    // const spy = jest
    //   .spyOn(api, 'fetchIngredients')
    //   .mockResolvedValue(mockIngredients);
    //await store.dispatch(getIngredients());
  });
});
